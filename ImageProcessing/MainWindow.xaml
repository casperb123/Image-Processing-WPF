<mah:MetroWindow x:Class="ImageProcessing.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:ImageProcessing"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 mc:Ignorable="d"
                 Title="Image Processing"
                 Height="705"
                 Width="1300">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="Button"
                   BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Padding"
                        Value="15,8" />
            </Style>
        </Grid.Resources>

        <Border BorderThickness="2"
                BorderBrush="Gray"
                Grid.Column="1"
                Margin="0,0,5,0">
            <Image x:Name="imageOriginal"
                   Grid.Column="1" />
        </Border>
        <Border BorderThickness="2"
                BorderBrush="Gray"
                Grid.Column="2"
                Margin="5,0,0,0">
            <Image x:Name="imageManipulated" />
        </Border>

        <Button x:Name="buttonOpen"
                Content="Open Image"
                Margin="0,0,10,0"
                VerticalAlignment="Top"
                Click="ButtonOpen_Click" />

        <Grid Grid.Row="1"
              Grid.Column="1"
              Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Rectangle x:Name="rectangleColorMin"
                       Stroke="Black"
                       Fill="Red"
                       Width="80"
                       Margin="0,0,5,0" />
            <Rectangle x:Name="rectangleColorMax"
                       Stroke="Black"
                       Fill="Red"
                       Grid.Column="1"
                       Width="80"
                       Margin="5,0,0,0" />

            <mah:RangeSlider x:Name="sliderHue"
                             VerticalAlignment="Center"
                             Minimum="0"
                             Maximum="360"
                             UpperValue="360"
                             Grid.Column="2"
                             Margin="10,0,0,0"
                             AutoToolTipPlacement="TopLeft"
                             mah:SliderHelper.EnableMouseWheel="MouseHover"
                             mah:SliderHelper.ChangeValueBy="LargeChange"
                             LargeChange="5"
                             LowerValueChanged="SliderHue_ValueChanged"
                             UpperValueChanged="SliderHue_ValueChanged">
                <mah:RangeSlider.AutoToolTipRangeValuesTemplate>
                    <DataTemplate DataType="mah:RangeSliderAutoTooltipValues">
                        <UniformGrid Columns="2"
                                     Rows="2">
                            <TextBlock HorizontalAlignment="Right"
                                       Text="From:" />
                            <TextBlock HorizontalAlignment="Right"
                                       Text="{Binding LowerValue, StringFormat='{}{0:N2}'}" />
                            <TextBlock HorizontalAlignment="Right"
                                       Text="To:" />
                            <TextBlock HorizontalAlignment="Right"
                                       Text="{Binding UpperValue, StringFormat='{}{0:N2}'}" />
                        </UniformGrid>
                    </DataTemplate>
                </mah:RangeSlider.AutoToolTipRangeValuesTemplate>
            </mah:RangeSlider>

            <StackPanel Orientation="Horizontal"
                        Grid.Column="2"
                        Grid.Row="1"
                        HorizontalAlignment="Center">
                <Button x:Name="buttonModify"
                        Content="Modify"
                        Click="ButtonModify_Click" />
            </StackPanel>

            <!--<StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal"
                            Height="35"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,5">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Padding"
                                    Value="20,0" />
                            <Setter Property="Margin"
                                    Value="5,0" />
                        </Style>
                    </StackPanel.Resources>

                    <Button x:Name="buttonRemoveRedSetPixel"
                            Content="Remove Red SetPixel"
                            Click="ButtonRemoveRedSetPixel_Click" />
                    <Button x:Name="buttonRemoveRedLockBits"
                            Content="Remove Red LockBits"
                            Click="ButtonRemoveRedLockBits_Click" />
                </StackPanel>
                <StackPanel Orientation="Horizontal"
                            Height="35"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,5">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Padding"
                                    Value="20,0" />
                            <Setter Property="Margin"
                                    Value="5,0" />
                        </Style>
                    </StackPanel.Resources>

                    <Button x:Name="buttonRemoveGreenSetPixel"
                            Content="Remove Green SetPixel"
                            Click="ButtonRemoveGreenSetPixel_Click" />
                    <Button x:Name="buttonRemoveGreenLockBits"
                            Content="Remove Green LockBits"
                            Click="ButtonRemoveGreenLockBits_Click" />
                </StackPanel>
                <StackPanel Orientation="Horizontal"
                            Height="35"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,5">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Padding"
                                    Value="20,0" />
                            <Setter Property="Margin"
                                    Value="5,0" />
                        </Style>
                    </StackPanel.Resources>

                    <Button x:Name="buttonRemoveBlueSetPixel"
                            Content="Remove Blue SetPixel"
                            Click="ButtonRemoveBlueSetPixel_Click" />
                    <Button x:Name="buttonRemoveBlueLockBits"
                            Content="Remove Blue LockBits"
                            Click="ButtonRemoveBlueLockBits_Click" />
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            Height="35"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,5">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Padding"
                                    Value="20,0" />
                            <Setter Property="Margin"
                                    Value="5,0" />
                        </Style>
                    </StackPanel.Resources>

                    <Button x:Name="buttonGraySetPixel"
                            Content="Gray SetPixel"
                            Click="ButtonGraySetPixel_Click" />
                    <Button x:Name="buttonGrayLockBits"
                            Content="Gray LockBits"
                            Click="ButtonGrayLockBits_Click" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Height="35">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Padding"
                                Value="20,0" />
                        <Setter Property="Margin"
                                Value="5,0" />
                    </Style>
                </StackPanel.Resources>

                <Button x:Name="buttonTiles"
                        Content="Tiles"
                        Click="ButtonTiles_Click" />
                <Button x:Name="buttonParallel"
                        Content="Parallel"
                        IsEnabled="False"
                        Click="ButtonParallel_Click" />
            </StackPanel>-->
        </Grid>
    </Grid>
</mah:MetroWindow>
